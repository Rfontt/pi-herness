---
description: Write the right planning artifact (PR/FAQ, design doc, or spec → plan → tasks) before code
---
Use the spec-driven-development skill. Pick the artifact by situation: PR/FAQ (product idea), design doc (change to existing system), or spec.md → plan.md → tasks.md (new feature). Explore first, write the artifact using its exact template, mark ambiguity with [NEEDS CLARIFICATION], never guess, and wait for approval before touching code.
