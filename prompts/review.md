---
description: Review staged changes before committing
argument-hint: "[scope]"
---
Use the code-review skill. Review the staged changes (`git diff --cached`) from both perspectives: correctness + business/architecture. List findings by severity (blocker / important / suggestion) with location and a concrete suggestion.
