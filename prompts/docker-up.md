---
description: Bring up the Docker Compose stack (colima + clean + docker-compose up -d)
---
Use the docker-compose-up skill. Start colima if it is not running, clean all Docker containers/images/volumes, then run `docker-compose up -d` and confirm the containers are healthy.
